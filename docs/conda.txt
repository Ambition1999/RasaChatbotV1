
conda create -n demochatbot_rasa python=3.7

conda create -n demochatbot_rasa_v1 python=3.7

conda env remove --name demochatbot_rasa 

pip install rasa-x --extra-index-url https://pypi.rasa.com/simple

https://restcountries.eu/rest/v2/name/vietnam

rasa run --endpoints endpoints.yml --credentials credentials.yml 

http://localhost:5005/webhooks/facebook/webhook

https://c49293b890f1.ngrok.io/webhooks/facebook/webhook

python manage.py startapp test_app

python .\manage.py runserver

rasa run -m models --enable-api --cors "*" --debug

rasa run --log-file out.log --cors * --enable-api

rasa run --log-file out.log --cors * --enableapi

=== Enable rest api without CORS
rasa run -m models --endpoints endpoints.yml --port 5002 --credentials credentials.yml

=== Enable rest api with CORS
rasa run -m models --endpoints endpoints.yml --port 5002 --credentials credentials.yml --cors “*”

rasa run -m models --endpoints endpoints.yml --credentials credentials.yml
